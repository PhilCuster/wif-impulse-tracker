import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'wif-impulse-tracker',
  webDir: 'build',
  bundledWebRuntime: false
};

export default config;
